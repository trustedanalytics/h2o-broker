broker.store:
  node: /test_node
  cluster: fakeZkCluster

zookeeper:
  metadataNode: /metadata
  credentialsNode: /credentials

h2o:
  provisioner:
    url: fakeUrl
    memory: fakeMemory
    nodes: fakeNodes
    timeout: 2

yarn.config: '{"HADOOP_CONFIG_KEY":{}}'

nats:
  url: fakeUrl
  serviceCreationTopic: fakeTopic

cf:
  servicename: fakeName
  serviceid: fakeId
  baseid: fakeBaseId

metadata:
  imageUrl: ${IMAGE_URL:}
